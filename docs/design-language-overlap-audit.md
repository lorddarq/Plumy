# Omvra design-language overlap audit

Scope: representative repository inventory for task `1782744396593`. The shared primitives are the intended canonical implementation; this report distinguishes updates in the current task from remaining migration owned by the follow-up.

| Intent | Evidence / observed variants | Recommendation | Risk | Owner / boundary | Current task / follow-up |
| --- | --- | --- | --- | --- | --- |
| Buttons | `src/app/components/ui/button.tsx`; local buttons in `TaskExecutionAction.tsx`, `goals/GoalsRuntimeStatus.tsx`, and other surface files use slate/amber/blue classes with different focus and disabled rules. | Current task updates `Button` and representative footer, Goals, and Settings consumers. Migrate remaining local actions to `Button` where semantics match; preserve execution tone only when product-specific. | Medium: visual density and destructive semantics can change. | Follow-up owns remaining local button migration. | Updated canonical primitive and representative consumers now; remaining locals follow up. |
| Inputs | Canonical `ui/input.tsx`; settings sections add repeated `FIELD_CLASS`/raw border, shadow, and focus values; several goal labels use native-looking wrappers. | Keep `Input` canonical, move shared field styling to tokens, migrate repeated settings/goal overrides. | Low–medium. | Follow-up owns inventory migration; current task updates foundation. | Foundation updated now; repeated local classes follow up. |
| Selects | Canonical Radix `ui/select.tsx`; `SettingsPanel.tsx`, goals, and agent settings add repeated trigger classes; one native `<select>` remains in `AgentRuntimeSettings.tsx`. | Use `Select` for custom option sets; preserve native select where browser semantics are explicitly preferred and document it. | Medium: Radix portal/layout differences. | Follow-up owns consumer-by-consumer migration. | Canonical trigger updated now; consumer migration follows up. |
| Labels | `ui/label.tsx` plus many inline `<label><span>` patterns in settings/goals. | Use `Label` for labelled controls; retain native label wrappers only when they enclose the control and have no independent styling need. | Low. | Follow-up. | Canonical label updated now; inline migration follows up. |
| Badges/status | `ui/badge.tsx`, `ProjectBadge.tsx`, `TaskSummarySection.tsx`, `GoalsPresentation.tsx`, and `GoalsRuntimeStatus.tsx` each define compact chips. | Keep status/project/health resolvers canonical; consolidate generic chip geometry into `Badge`, preserving semantic resolver output. | Medium: status contrast and meaning must not regress. | Current task preserves resolvers; follow-up consolidates generic chips. | Badge geometry updated now; resolver-backed duplicates follow up. |
| Notices | `ui/alert.tsx`, `ExecutionNotice.tsx`, and inline amber/red/slate notice blocks across goals and settings. | Use `Alert` for persistent notices; preserve execution notices when they expose workflow-specific next steps. | Medium: live execution copy/layout. | Follow-up. | Alert foundation updated now; notice migration follows up. |
| Surfaces / panel sections | `ui/card.tsx`, `AnchoredPanel.tsx`, `SettingsPanel.tsx`, and repeated `rounded-* border ... bg-*` blocks. | Current task adds `.omvra-panel-section` and token-backed Card; migrate only same-intent sections. | Medium: anchored-panel geometry is product-specific. | Follow-up; preserve UI 2.0 anchored panels. | Card/panel contract established now; anchored-panel audit follows up. |
| Action bars / footers | `TaskFooterActions.tsx`, dialog/sheet footers, and local flex rows. | Current task adds `.omvra-action-bar` to task footer; migrate same-intent footer rows, not toolbars with distinct behavior. | Low. | Follow-up. | Task footer updated now; remaining rows follow up. |
| Neutral / semantic tokens | Existing `theme.css` generic tokens, raw Tailwind colors, and resolver-owned status colors coexist. | `design-tokens.css` is the Omvra semantic layer. Do not replace resolver mappings or force product-specific colors into neutral tokens. | Low now; drift risk later. | Current task establishes source; follow-up removes accepted raw duplicates. | Token namespace and feedback pairs established now; raw duplicates follow up. |

## Explicit residual decisions

The current task does not rewrite every consumer: the audit is intentionally inventory-driven and the follow-up is the migration boundary. Inline styles that encode status/project/health remain valid only through the existing resolver output. Execution supervision, timeline, and anchored-panel controls may keep local variants where behavior or density is materially different; each migration should record that reason in its PR.
